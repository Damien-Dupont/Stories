# Stories

Plateforme de lecture interactive (Laravel + React + Docker)

## Installation

1. Copier `.env.example` vers `.env`
2. Ajuster les valeurs
3. Lancer `docker-compose up -d` ou `docker-compose --env-file .env.prod up -d`
